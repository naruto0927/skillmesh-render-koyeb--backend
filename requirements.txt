# ─── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ─── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9  # Used by Alembic for migrations (sync)

# ─── Validation & settings ─────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ─── Security / auth ───────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# ─── HTTP client (for Supabase calls if needed) ────────────────────────────────
httpx==0.27.0

# ─── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0          # Used by TestClient
anyio==4.3.0

# ─── Dev utilities ─────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ─── Test DB (SQLite async for fast unit tests) ────────────────────────────────
aiosqlite==0.20.0

# ─── Email validation (required by Pydantic EmailStr) ─────────────────────────
email-validator==2.1.1
