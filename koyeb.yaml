name: skillmesh-api
services:
  - name: api
    type: web
    git:
      branch: main
    build:
      buildpack: python
    run:
      command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - port: 8000
        protocol: http
    health_checks:
      - path: /api/v1/health
        port: 8000
    env:
      - name: ENVIRONMENT
        value: production
      - name: DATABASE_URL
        secret: true       # Set in Koyeb dashboard → Secrets
      - name: SUPABASE_URL
        secret: true
      - name: SUPABASE_ANON_KEY
        secret: true
      - name: SUPABASE_SERVICE_ROLE_KEY
        secret: true
      - name: JWT_SECRET
        secret: true
      - name: CORS_ORIGINS
        secret: true
